name: Finlog cd
on:
  push:
    branches:
      - [main]

jobs:
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    needs: []

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway
        run: railway up --service finlog
        env:
          RAILWAY_TOKEN: ${{ secrets.RaILWAY_TOKEN }}
